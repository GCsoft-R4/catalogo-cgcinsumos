services:
  backend:
    build: ./backend
    container_name: catalogoweb_backend
    restart: unless-stopped
    depends_on:
      - ollama
    ports:
      - "5000:5000"
    volumes:
      - ./backend/uploads:/app/uploads
    env_file:
      - ./backend/.env
    environment:
      NODE_ENV: production
